function checkIfIdentical(firstString: string, secondString: string) {
    let isIdentical: boolean = false

    if (firstString === secondString) {
        isIdentical = true
    }

    return isIdentical
}

export default checkIfIdentical
